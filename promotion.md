# 🕐 csespusher —— 一键分发全校课表，还能让软件时间和打铃对上

> 一站式 CSES / ClassIsland / ClassWidgets 课程表配置分发平台。
> 管理员上传一张 Excel，全班打开链接直接导入。

---

## 它解决两个真实痛点

### 痛点一：课表分发，又散又烦

每学期都要把课表做成三种格式、挨个发到每个班？
换个格式、改个时间线，全班又得重发一遍？

**csespusher 的做法：**
学校管理员上传一张格式化 Excel（科目、时间线、年级课表都在里面），
平台自动生成三种软件的配置文件；每个班级一个**固定分享链接**，
学生打开就能下载、或者复制直链直接「从互联网导入」。
课表更新了？链接不变，学生重新拉一次就行。

### 痛点二：软件上的时间，永远比铃声快两分钟

学校的打铃母钟每天偷偷走偏几秒，一个学期攒下来，
软件里倒计时明明还没下课，铃声早就响了。

**csespusher 的做法：**
为每所学校开一个**独立的时间源**——对外广播的时间 = 真实时间 + 该校累积偏移。
管理员只要设好「每天走快/走慢几秒」，系统会持续跟踪漂移；
发现偏差了，对着学校时钟填一下表盘读数，**一键校准**。
学生机同步后，软件时间从此跟着铃声走。

---

## 功能速览

### 📋 Excel 一键转三种格式

| 格式 | 输出 | 说明 |
| --- | --- | --- |
| **CSES v2** | YAML | 符合 CSES schema，含每周 schedules |
| **ClassIsland** | JSON | 完整 profile（时间线 + 课表 + 科目），导入即用 |
| **ClassWidgets** | JSON | v1 schedule 格式，配合软件内置导入 |

- 一个 Excel = 全校课表：每个 sheet 一个年级，`config` sheet 放科目 / 时间线 / 杂项配置
- 每天支持独立时间线（最多七条），同一天的多段时间流放进同一条
- 杂项配置（ClassIsland 主题、ClassWidgets 提示音等）随课表一起分发，也能在线编辑

### 🔗 链接体系：固定、唯一、免维护

- **班级分享页** `/share/{token}`：课表预览 + 三种格式下载 + 直链
- **配置直链** `/api/public/classes/{id}/raw/{格式}`：带 CORS，
  直接粘贴进软件的「从互联网导入配置」，永久有效
- 游客主页按 **省 / 市 / 校** 分组浏览，点开班级即可预览课表

### 🕐 每校独立 NTP 时间同步（新功能）

- **每日偏移量**（秒/天，正负皆可）：填一次，系统按线性漂移持续累积
- **手动校准**：填学校时钟当前读数，一键抹平累计误差，每日偏移量保持不变
- **双通道客户端**：标准 NTP（UDP，每校独占端口）+ HTTP 校时接口（返回含偏移的时间戳）
- 管理页实时显示当前偏差与学校时间；超管能看全校各端口的运行状态
- 班级页直接把 NTP 地址展示给学生复制

### 👥 权限与协作

- **游客**：浏览学校、预览课表、下载配置
- **普通管理员**：邮箱验证码登录，创建学校、上传课表、管理协作成员
- **超级管理员**：用户管理 + 平台运行状态

---

## 三分钟跑起来

### Docker（推荐）

```bash
docker build -t csespusher .
docker run -d -p 8765:8765 -p 11123-11378:11123-11378/udp csespusher
```

### 本地开发

```bash
# 后端
cd backend
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt
copy .env.example .env
.venv/Scripts/python -m uvicorn app.main:app --port 8765

# 前端（开发模式，生产模式下由后端直接托管）
cd frontend
npm install
npm run dev
```

打开 `http://127.0.0.1:8765/`，注册账号 → 创建学校 → 上传 Excel → 生成班级链接。
（登录验证码没配 SMTP 时会直接打印在后端控制台，零配置就能玩。）

---

## 最后

如果你们的课表还在靠群文件和 U 盘流转，或者软件时间永远比铃声快几分钟——
这个项目就是为你准备的。

⭐ 觉得有用就点颗星；问题与建议欢迎来 GitHub Issue / 论坛回帖聊。

> 本帖基于 csespusher v0.1.0 撰写。Excel 格式约定、NTP 部署细节与
> Windows w32time 的端口限制等，详见项目 README。
